# 🔧 Деплой исправлений: Страница Профиля

**Дата**: 2025-11-04
**Версия**: 1.4

---

## ✅ Что исправлено в профиле:

### 1. ✅ Реальный рейтинг пользователя
**Проблема**: Внизу в секции "Прогресс Ранга" были заглушки (жестко закодированные значения)

**Решение**:
- Добавлена загрузка позиции пользователя в лидерборде через `/api/leaderboard/rank`
- Секция "Прогресс Ранга" теперь полностью динамическая
- Отображаются все ранги с правильными состояниями:
  - ✓ Пройденные ранги (opacity 50%, серый чек)
  - 🟡 Текущий ранг (жёлтая рамка, прогресс-бар)
  - ⚪ Будущие ранги (серый текст)
- Прогресс-бар показывает реальный прогресс до следующего ранга
- Отображается "🏆 #X в мире" под именем пользователя

### 2. ✅ Кнопка "Копировать" под ссылкой
**Проблема**: Кнопка была справа от реферальной ссылки

**Решение**:
- Кнопка теперь полностью ПОД инпутом
- Кнопка на всю ширину блока
- Добавлен эмодзи 📋 для наглядности
- Инпут теперь с рамкой (border) для лучшего визуала

### 3. ✅ Реальные события
**Проблема**: Внизу были заглушки с фейковыми событиями

**Решение**:
- Загружаются реальные события, где пользователь сделал ставки
- Отображаются последние 10 событий
- Показывается название события, позиция (ДА/НЕТ), сумма ставки
- Правильные статусы: ВЫИГРЫШ (зелёный), ПРОИГРЫШ (красный), ОЖИДАНИЕ (жёлтый)
- Для выигрыша показывается "+X PRED"
- Для проигрыша "-X PRED"
- Для ожидания "Потенциал: +X PRED"

---

## 📦 Измененные файлы:

```
webapp/templates/profile.html           ✅ Весь функционал профиля обновлён
```

**Все изменения в одном файле!**

---

## 🚀 Инструкция по деплою:

### 1. На локальной машине - Закоммитить изменения

```bash
cd /Users/alluc/Documents/ThePred

# Добавить изменённый файл
git add webapp/templates/profile.html DEPLOY_PROFILE_FIXES.md

# Создать коммит
git commit -m "feat: Profile page improvements

- Display real leaderboard rank (#X в мире)
- Make rank progress section dynamic based on user data
- Move 'Copy' button below referral link (full width)
- Display real user events (with bets) instead of placeholders
- Add progress bar showing progress to next rank
- Style improvements for referral section

Closes profile issues:
- Fake rank progress data
- Copy button positioning
- Placeholder events"

# Запушить
git push origin main
```

### 2. На сервере - Обновить код

```bash
ssh root@твой_ip
cd /root/ThePred

# Подтянуть изменения
git pull origin main
```

### 3. Перезапустить webapp

```bash
# Перезапустить только webapp (не нужно пересобирать!)
docker-compose restart webapp

# Проверить что запущен
docker-compose ps webapp
```

**Примечание**: Backend НЕ МЕНЯЛСЯ, поэтому можно перезапустить только webapp!

### 4. Проверить логи

```bash
# Логи webapp
docker-compose logs -f webapp | tail -30
```

---

## 🧪 Тестирование:

### Тест 1: Рейтинг в мире

1. Открой профиль: https://thepred.tech/profile
2. **Ожидается**: Под именем пользователя показывается "🏆 #X в мире" ✅
3. **Было**: Пусто или заглушка ❌

**Если не показывается**:
- Открой F12 → Console
- Смотри ошибки при вызове `/api/leaderboard/rank`
- Проверь логи backend: `docker-compose logs backend | grep leaderboard`

### Тест 2: Прогресс Ранга (динамический)

1. Открой профиль: https://thepred.tech/profile
2. Прокрути вниз до секции "🎖 Прогресс Ранга"
3. **Ожидается**:
   - Пройденные ранги (ниже текущего) показывают "✓ ПРОЙДЕН" и затемнены (opacity 50%) ✅
   - Текущий ранг с жёлтой рамкой, надписью "ТЕКУЩИЙ", прогресс-баром ✅
   - Прогресс-бар показывает реальный процент (например "156 / 500 прогнозов (344 до Алмаза)") ✅
   - Будущие ранги (выше текущего) серые, без рамки ✅
4. **Было**: Все ранги одинаковые, жёстко закодированные значения ❌

**Пример для пользователя с рангом Bronze и 35 ставками**:
- 🥉 Бронза: border-2 border-yellow-400, "ТЕКУЩИЙ", прогресс-бар 35%
- 🥈 Серебро: серый, "100-500 прогнозов"
- 🥇 Золото: серый, "500-2000 прогнозов"
- 💎 Алмаз: серый, "2000-10000 прогнозов"
- 🌟 Легенда: серый, "10000+ прогнозов"

### Тест 3: Кнопка "Копировать" под ссылкой

1. Открой профиль → секция "🎁 Пригласить Друзей"
2. **Ожидается**:
   - Реферальная ссылка в инпуте (с рамкой) ✅
   - ПОД инпутом кнопка "📋 Копировать Ссылку" на всю ширину ✅
3. **Было**: Кнопка справа от инпута в одной строке ❌

3. Нажми кнопку "📋 Копировать Ссылку"
4. **Ожидается**: Toast notification "Скопировано!" ✅
5. Вставь где-нибудь (Ctrl+V) - должна быть ссылка `https://t.me/The_Pred_Bot?start=123456` ✅

### Тест 4: Реальные события

1. Открой профиль → секция "📜 Последние Прогнозы"
2. **Если у пользователя НЕТ ставок**:
   - **Ожидается**: Пустой список (заглушки должны исчезнуть) ✅

3. **Если у пользователя ЕСТЬ ставки**:
   - **Ожидается**: Список реальных событий, где он делал ставки ✅
   - Каждое событие показывает:
     - Название события ✅
     - "Прогноз ДА • 1000 PRED" (или НЕТ) ✅
     - Статус: ВЫИГРЫШ (зелёный) / ПРОИГРЫШ (красный) / ОЖИДАНИЕ (жёлтый) ✅
     - Для выигрыша: "+1850 PRED" ✅
     - Для проигрыша: "-1000 PRED" ✅
     - Для ожидания: "Потенциал: +1500 PRED" ✅

4. **Было**: Фейковые события "Bitcoin > $100k?", "Man City wins PL?" и т.д. ❌

**Если события не загружаются**:
- Открой F12 → Console
- Смотри ошибки при вызове `/api/bets/history`
- Проверь логи webapp: `docker-compose logs webapp | grep bets`

---

## 🎨 UI Изменения:

### Реферальная секция (ДО):
```
┌─────────────────────────────────────────────────┐
│ Ваша Реферальная Ссылка                         │
│ [https://t.me/...] [Копировать]                 │
└─────────────────────────────────────────────────┘
```

### Реферальная секция (ПОСЛЕ):
```
┌─────────────────────────────────────────────────┐
│ Ваша Реферальная Ссылка                         │
│ ┌─────────────────────────────────────────────┐ │
│ │ https://t.me/The_Pred_Bot?start=123456      │ │
│ └─────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────┐ │
│ │       📋 Копировать Ссылку                  │ │
│ └─────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────┘
```

### Прогресс Ранга (ДО):
```
Все ранги одинаковые, жёстко закодированные
🥉 Бронза (opacity 50%)
🥈 Серебро (opacity 50%)
🥇 Золото (border yellow, "ТЕКУЩИЙ", прогресс-бар 31%)
💎 Алмаз (обычный)
🌟 Легенда (обычный)
```

### Прогресс Ранга (ПОСЛЕ):
```
Динамически рендерится на основе текущего ранга пользователя

Если пользователь Bronze (35 ставок):
🥉 Бронза (border yellow, "ТЕКУЩИЙ", прогресс-бар 35%, "35 / 100 прогнозов (65 до Серебра)")
🥈 Серебро (обычный, "100-500 прогнозов")
🥇 Золото (обычный, "500-2000 прогнозов")
💎 Алмаз (обычный, "2000-10000 прогнозов")
🌟 Легенда (обычный, "10000+ прогнозов")

Если пользователь Gold (1200 ставок):
🥉 Бронза (opacity 50%, "✓ ПРОЙДЕН")
🥈 Серебро (opacity 50%, "✓ ПРОЙДЕН")
🥇 Золото (border yellow, "ТЕКУЩИЙ", прогресс-бар 47%, "1200 / 2000 прогнозов (800 до Алмаза)")
💎 Алмаз (обычный, "2000-10000 прогнозов")
🌟 Легенда (обычный, "10000+ прогнозов")
```

---

## 🐛 Troubleshooting:

### Проблема: Рейтинг не показывается (пусто под именем)

**Решение**:
1. Проверь логи webapp:
```bash
docker-compose logs webapp | grep "leaderboard/rank"
```

2. Проверь что эндпоинт работает:
```bash
curl http://localhost:8001/api/leaderboard/rank
```

3. Если 401 ошибка - пользователь не залогинен
4. Если 404 - эндпоинт не существует (проверь backend)

### Проблема: Прогресс Ранга всё равно жёстко закодирован

**Решение**:
1. Открой F12 → Console
2. Смотри вызов функции `updateRankProgress()`
3. Должно быть: `updateRankProgress('Bronze', 35)` (или другой ранг)

Если функция не вызывается:
```bash
# Проверь что файл обновился
grep "updateRankProgress" /root/ThePred/webapp/templates/profile.html
```

Если нет - файл не обновился:
```bash
git pull origin main
docker-compose restart webapp
```

### Проблема: Кнопка "Копировать" всё равно справа

**Решение**:
```bash
# Проверь HTML
grep -A 5 "referral-link" /root/ThePred/webapp/templates/profile.html
```

Должно быть:
```html
<input type="text" readonly value="..." id="referral-link"
       class="w-full bg-transparent ...">
<button onclick="copyReferralLink()" class="w-full gold-gradient ...">
```

Если нет:
```bash
git pull origin main
docker-compose restart webapp
```

### Проблема: События всё равно фейковые

**Решение**:
1. Открой F12 → Console
2. Смотри вызов `loadUserMarkets()` вместо `loadBetHistory()`
3. Должно быть: `loadUserMarkets()` в `DOMContentLoaded`

Если видишь `loadBetHistory()`:
```bash
# Проверь что файл обновился
grep "loadUserMarkets" /root/ThePred/webapp/templates/profile.html
```

---

## ✅ Чеклист перед тестированием:

- [ ] Закоммичены и запушены изменения
- [ ] На сервере выполнен `git pull origin main`
- [ ] Webapp перезапущен: `docker-compose restart webapp`
- [ ] Webapp в статусе "Up": `docker-compose ps webapp`
- [ ] Открыт профиль в браузере

---

## 📝 После успешного деплоя:

Скинь скриншоты:
1. ✅ Рейтинг "#X в мире" под именем пользователя
2. ✅ Секция "Прогресс Ранга" с динамическими данными (текущий ранг с прогресс-баром)
3. ✅ Реферальная секция с кнопкой ПОД инпутом (full width)
4. ✅ Секция "Последние Прогнозы" с реальными событиями (или пустая, если нет ставок)

---

## 🎯 Краткая сводка изменений:

| Элемент | Было | Стало |
|---------|------|-------|
| Рейтинг в мире | Пусто или заглушка | 🏆 #X в мире (динамически) |
| Прогресс Ранга | Жёстко закодирован (Gold, 31%) | Динамический (текущий ранг, реальный %) |
| Кнопка "Копировать" | Справа от инпута | ПОД инпутом (full width) |
| События | Фейковые заглушки | Реальные события с ставками |

---

**Удачного деплоя!** 🚀

**P.S.**: Все изменения только в frontend (profile.html), backend не трогали!
